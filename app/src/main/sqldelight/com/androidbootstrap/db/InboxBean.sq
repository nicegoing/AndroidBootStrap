import java.util.Calendar;

CREATE TABLE InboxBean(
  _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  expected_result TEXT NOT NULL,
  todo TEXT NOT NULL,
  add_time INTEGER AS Calendar NOT NULL,
  todo_time INTEGER AS Calendar,
  type TEXT NOT NULL

);

select_all:
select * from InboxBean;